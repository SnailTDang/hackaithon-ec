# .env for nextjs-app (compatible with Fastify server)
# Example environment variables, adjust as needed for your project

# Node environment
NODE_ENV=development

# Fastify server URL (used for API calls from Next.js frontend)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Session secret (should match Fastify session config if used client-side)
SESSION_SECRET="a secret with minimum length of 32 characters"

# i18n default locale
NEXT_PUBLIC_DEFAULT_LOCALE=vi

# Add other variables as needed
